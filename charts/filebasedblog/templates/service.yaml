apiVersion: v1
kind: Service
metadata:
  name: {{ include "filebasedblog.fullname" . }}
  labels:
    {{- include "filebasedblog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "filebasedblog.selectorLabels" . | nindent 4 }}
